#include <stdio.h>
#include<math.h>
int main() 
{
    int a;
    float sqrt_rt;
    printf("Enter the number\n");
    scanf("%d",&a);
    sqrt_rt = pow(a,0.5);
    printf("The value is %d=%f",a,sqrt_rt);
    return 0;
}