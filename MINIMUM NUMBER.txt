// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b,c;
    printf("Enter the numbers");
    scanf("%d%d%d",&a,&b,&c);
    if (a<b && a<c)
    {
        printf("Number a is least");
    }
    else if(b<a && b<c)
    {
        printf("Number b is least");
    }
    else 
    {
        printf("Number c is least");
    }
    return 0;
}